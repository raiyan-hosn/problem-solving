#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
int n,t;
ll sum(vector<int>&v){
    ll sm=0;
    for(int i=0;i<v.size();i++)
        sm+=v[i];
    return sm;
}
struct track
{
    int index,duration;
};
vector<int> listBestTracks(vector<int>&v,int idx){

}
int main(void)
{
    while (scanf("%d %d",&n,&t)!=EOF)
    {
        vector<int>v(t);
        for(int i=0;i<t;i++){
            scanf("%d",&v[i]);
        
        }
    }
    
}